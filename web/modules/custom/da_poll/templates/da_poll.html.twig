{#
/**
 * @file da_poll.html.twig
 * Default theme implementation to present Poll data.
 *
 * This template is used when viewing Poll pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_da_poll()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('da_poll') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
